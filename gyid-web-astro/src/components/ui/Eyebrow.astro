---
interface Props {
  class?: string;
}

const { class: cls = '' } = Astro.props;
---

<p class:list={['font-mono text-xs font-bold uppercase tracking-[0.25em] text-cyber-blue', cls]}>
  <slot />
</p>
