# 2026-07-15 未完成工作清单

## 概述

今日工作涉及三个主要任务：GitHub 首次推送准备、网站下载链接修复、二进制安装包构建。其中 GitHub 推送已完成，但网站部署和安装包构建存在多个未完成项。

---

## 🔴 高优先级 — 阻塞性问题

### 1. 服务器不可达，网站无法部署
- **问题**：服务器 `8.136.182.127` SSH 端口 22 连接超时
- **影响**：网站源码已修复但无法部署到线上，www.geoyuan.com 仍显示旧版页面
- **待办**：检查服务器状态（阿里云控制台），恢复 SSH 服务后执行部署
- **部署命令**（服务器恢复后）：
  ```bash
  cd geoyuan-web
  bash deploy.sh --server 8.136.182.127 --skip-build
  ```

### 2. 网站修改未提交到 Git
- **问题**：11 个网站文件已修改但未 commit
- **涉及文件**：
  - `geoyuan-web/deploy.sh`（修复路径 bug）
  - `geoyuan-web/src/components/Footer.astro`
  - `geoyuan-web/src/i18n/en.ts`、`zh.ts`
  - `geoyuan-web/src/layouts/Layout.astro`
  - `geoyuan-web/src/pages/about/index.astro`、`docs/index.astro`、`download/index.astro`
  - `geoyuan-web/src/pages/zh/about.astro`、`docs.astro`、`download.astro`
- **待办**：`git add -A && git commit -m "fix: website download links and GitHub URLs"`

### 3. 下载页引用了不存在的包名 `gyid-cli`
- **问题**：`gyid-cli` 已从 workspace 移除，被 `geoyuan-cli` 替代
- **当前 workspace 成员**：`geoyuan-core`, `geoyuan-cli`, `geoyuan-gui`, `gyid-core`, `gyid-gui`
- **影响**：下载页的 `cargo install gyid-cli` 命令无法执行
- **待办**：将下载页所有 `gyid-cli` → `geoyuan-cli`，`gyid` → `geoyuan`（CLI 命令部分）

### 4. 版本号不一致
- **问题**：网站显示 `v0.4.1`，实际 Cargo.toml 中所有包版本为 `0.1.0`
- **影响**：下载文件名和安装指南中的版本号错误
- **待办**：统一版本号（更新 Cargo.toml 到 0.4.1，或更新网站到 0.1.0）

---

## 🟡 中优先级 — 二进制安装包

### 5. Windows CLI 二进制未构建成功
- **问题**：`cargo build --release -p geoyuan-cli` 构建失败（libsqlite3-sys 编译错误）
- **已有产物**：仅 `target/release/geoyuan-gui.exe`（Slint GUI）
- **待办**：
  - 排查 libsqlite3-sys 构建错误（可能需要设置 `CC` 环境变量指向 MinGW gcc）
  - 构建成功后将 `.exe` 复制到 `geoyuan-web/public/downloads/`
  - 建议文件名：`geoyuan-cli-v0.1.0-x64.exe`

### 6. Windows GUI 安装包未制作
- **问题**：`geoyuan-gui.exe` 已编译但未打包为安装程序
- **待办**：
  - 考虑使用 Inno Setup 或 NSIS 制作 `.exe` 安装包
  - 或直接提供 `geoyuan-gui.exe` 作为绿色版

### 7. Linux / macOS 二进制无法在 Windows 交叉编译
- **问题**：Windows 上无法直接构建 Linux/macOS 二进制
- **待办**：
  - 方案 A：在 GitHub Actions 中配置 CI/CD 自动构建多平台二进制
  - 方案 B：在对应平台机器上分别构建
  - 方案 C：暂时只提供 Windows 下载 + 源码编译指南

### 8. GitHub Releases 未创建
- **问题**：下载链接指向 `github.com/huanghe2026/GYID/releases/latest/download/...` 但无 Release 存在
- **影响**：所有下载链接仍为 404
- **待办**：构建二进制后，在 GitHub 上创建 Release 并上传产物

---

## 🟢 低优先级 — 改进项

### 9. Cargo.lock 仍被 .gitignore 忽略
- **问题**：项目含二进制目标（CLI/GUI），通常应追踪 Cargo.lock 保证可复现构建
- **待办**：从 .gitignore 移除 `Cargo.lock`，执行 `git add Cargo.lock`

### 10. 下载页 `ready` 状态需更新
- **问题**：所有下载项 `ready: false`（UI 显示为禁用）
- **待办**：二进制构建并上传后，将对应项改为 `ready: true`

### 11. 网站下载页文件名前缀需统一
- **当前状态**：下载页用 `gyid-` 前缀，但实际 CLI 包名是 `geoyuan-cli`，二进制名是 `geoyuan`
- **待办**：决定统一命名策略：
  - 方案 A：全用 `geoyuan-` 前缀（与实际包名一致）
  - 方案 B：保留 `gyid-` 前缀（品牌统一），但需同步更新 CLI 命令

### 12. deploy.sh 路径 bug 已修复但未提交
- **问题**：`WEB_DIR="$SCRIPT_DIR/geoyuan-web"` 应为 `WEB_DIR="$SCRIPT_DIR"`（脚本已在 geoyuan-web 目录内）
- **状态**：已修复，包含在未提交的 11 个文件中

---

## ✅ 今日已完成

| # | 任务 | 状态 |
|---|------|------|
| 1 | Git 历史清洗 + LICENSE 创建 | ✅ 已完成 |
| 2 | 移除 9 个临时脚本 + 参数化服务器 IP | ✅ 已完成 |
| 3 | GitHub 仓库推送（huanghe2026/GYID，Private） | ✅ 已完成 |
| 4 | 网站下载链接修复（源码层面） | ✅ 已完成 |
| 5 | 网站 GitHub URL 修正（10 处） | ✅ 已完成 |
| 6 | 网站 `npm run build` 验证通过 | ✅ 已完成 |
| 7 | deploy.sh 路径 bug 修复 | ✅ 已完成 |

---

## 建议执行顺序

1. **提交网站修改**到 Git（2 分钟）
2. **排查 geoyuan-cli 构建错误**（可能 10-30 分钟）
3. **构建 Windows 二进制**并放入 `public/downloads/`（构建成功后 1 分钟）
4. **更新下载页**：版本号 `0.4.1` → `0.1.0`，包名 `gyid-cli` → `geoyuan-cli`，`ready: true`（10 分钟）
5. **重新构建网站** `npm run build`（1 分钟）
6. **提交并推送**到 GitHub（2 分钟）
7. **服务器恢复后部署**到 www.geoyuan.com

---

*生成时间：2026-07-15 07:45*
