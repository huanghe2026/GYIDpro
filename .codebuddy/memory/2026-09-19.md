# 2026-09-19

## 接手项目 + 完成 W6 收尾（Web 演示页 GPX/照片轨迹手动导入）

### 基线盘点
- `cargo test --workspace` 全绿（含 `trip-server/tests/three_party_flow.rs` 4 个用例）。
- 判断：路线图 W1–W6 已完成（trip-core / trip-cli / engine / trip-server / gyid-wasm / gyid-web）。
- 用户拍板下一步：**收尾 W6**（补齐"手动上传 GPX/照片轨迹"验收项）。

### 交付物
| 文件 | 内容 |
|---|---|
| `gyid-web/src/lib/importers.ts`（新增） | GPX 解析（trkpt/rtept/wpt，缺 `<time>` 按间隔合成，下限 300s）；JPEG EXIF 解析（自写 TIFF/IFD 解析器：GPSLatitude/Longitude + Ref、GPSTimeStamp+DateStamp、回退 DateTimeOriginal，含填充字节/大小端处理）。零依赖，仅产出 (lat,lng,ts)。 |
| `gyid-web/src/stores/chain.ts`（改造） | 新增 `CHAIN_*` 常量、`ImportSkipReason/ImportSkip/ImportResult/ImportPreview`、**纯函数 `planImport`**（§4 链规则：同 cell / cell 上限 10 / <300s / 非探索 <900s / 时间回退）、`buildPlan`、`previewImport`（只算不签）、`importCrumbs`（量化→过滤→逐条签名→末尾一次性落盘，index/prev_hash 自动续链，空链首条传 null/null）。 |
| `gyid-web/src/pages/Import.tsx`（新增） | Import 页：选 GPX/照片 → 预演（接受/丢弃 + 原因分布 + 时间范围）→ 导入入链 → 上传 Verifier → 全链自检 + 地图/日志。 |
| `gyid-web/src/App.tsx`（改造） | 新增 `/import` 路由与导航项（导航变 6 项）。 |

### 验证结果（实证，非仅编译）
- `pnpm typecheck` 通过、`pnpm build` 通过（46 模块）。
- **EXIF 字节级往返**：构造合成 JPEG/APP1/TIFF，解析出 lat=39.904166…、lng=116.408333…、ts=Date.UTC(2024,4,1,10,20,30)；S/W 取负正确；无 EXIF 返回 null。
- **importCrumbs 端到端**（Node + 真实 gyid-wasm，`initSync` 后 `loadWasm()` 命中缓存）：
  - 480 点 @60s（8h）→ 接受 **32** 条（= 28800/900），`verify_chain` = true；
  - 续链再导 120 点 → 新增 8 条，链长 40，index 连续，验链 true；
  - 导入早于链尾 → added=0，reason=`time-backwards`；
  - 探索会话 400s 间隔接受 2 条，非探索只接受 1 条（reason=`too-soon`）。
- 临时校验脚本已删除。

### 尚未做 / 后续
- W1–W6 的 git 整理提交（工作区仍有 207 删除 + 多目录未跟踪）。
- W7（GeoLife/MDC 标定）、W8（did:geoyuan + Base 锚定）、W10（TRIP-Arena）等仍未开始。
- Import 页未做浏览器真人操作测试（需浏览器 + 真实 GPX/照片样本）。

---

## 追加：W8（TIT + did:geoyuan）Rust 侧完成

### 交付物
| 文件 | 内容 |
|---|---|
| `trip-core/src/did.rs`（新增） | `did:geoyuan:z<base58btc(pubkey32)>` 编解码；W3C DID Core `DidDocument`（`@context`/`verificationMethod(publicKeyMultibase)`/`authentication`/`assertionMethod`/`service`/`alsoKnownAs`/`updated`）+ `DidDocumentConfig` + `AnchorReference`(CAIP-2 `eip155:chain:registry`)；JSON 生成/解析 + id↔verificationMethod 自洽校验；常量 `SERVICE_TIT/VERIFIER/P2P/ANCHOR`。 |
| `trip-core/src/tit.rs`（新增） | TIT 字段 0..=9（版本/身份公钥/epochs/breadcrumbs/unique_cells/trust/issued_at/validity/issuer(0 unsigned,1 identity,2 verifier)/签名）；确定性 CBOR + Base64url(URL_SAFE_NO_PAD)；`issue_identity_signed` / `issue_verifier_signed` / `verify(verifier_pubkey, now)`；`validate()`（trust∈[0,100]、unique≤breadcrumbs、unsigned 签名须全零）；`did()`、`meets_handle_threshold()`。**字段编号是 GYIP-0003 内部约定（-04 未规定 TIT 编号），已用黄金向量锁定，待 -05 重新 pin。** |
| `trip-core/tests/tit_golden.rs`（新增） | 锁定 signable/cbor/base64url/DID 精确字节；`did::encode([0;32])` = `did:geoyuan:z` + 32×'1'。 |
| `trip-cli/src/main.rs` | `gyid did show --seed .. [--verifier --p2p --handle --anchor]`、`gyid did resolve <did>`、`gyid tit issue [--issuer identity\|verifier --identity ..]`、`gyid tit verify <base64url\|hex> [--verifier-pubkey --now]`。 |
| `trip-server` | `GET /v1/did/:did`（`application/did+json`）；`GET /v1/tit/:hex`（复用 `engine_bridge::evaluate_identity` → `issue_tit`，spawn_blocking）；新配置 `TRIP_PUBLIC_URL`、`TRIP_ANCHOR`；新增 `engine_bridge::issue_tit`。 |
| `trip-server/tests/did_tit.rs`（新增） | DID 文档自洽 + 非法 DID 400；evidence 分页(300+213)上传 → TIT 签发 → `/.well-known` 公钥验签通过 / 错公钥与过期拒绝 / 404 / 400。 |
| `trip-server/README.md` | 补两个端点 + 两个环境变量文档。 |

### 依赖与错误类型变更
`trip-core` 新增 `base58`(workspace)、`base64 0.22`、`serde`(workspace)、`serde_json`(workspace)；`TripError` 新增 `InvalidDid` / `InvalidTit`。

### 验证
- `cargo test --workspace` 全绿。
- `cargo clippy -p trip-core -p trip-cli -p trip-server --all-targets -- -D warnings` 通过；顺手修掉 trip-cli 两处 clippy（既有 `implicit_saturating_sub`、我引入的 `manual_map`）。
- `cargo fmt -p trip-core -p trip-cli -p trip-server -- --check` 通过（已执行 fmt，主要动 trip-cli 既有风格）。
- CLI 实测：`did show` 输出 DID + 完整 DID Document（`#tit`/`#verifier`/`#anchor=eip155:8453:0xGeoTITRegistry`）；`tit issue` 的 base64url 与 core 黄金向量**逐字节一致**；`did resolve`、`tit verify` 通过。

### 踩坑备忘
- 同一文件**不要在同一条消息里并行下发多个 edit** —— 后一个 edit 基于旧快照会把前一个改动回滚（本次 `implicit_saturating_sub` 修复被回滚过一次）。

### 未做（W8 剩余）
- `GeoTITRegistry.sol` 合约 + Base 测试网锚定（`register` / `anchorEpoch` / `claimHandle`）→ W8 验收里"epoch 根可在 basescan 查"尚未满足。
- `gyid-wasm` / `gyid-web` 未暴露 DID/TIT（需 wasm-pack 重建，属后续）。

