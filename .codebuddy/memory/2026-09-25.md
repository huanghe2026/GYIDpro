# 2026-09-25 工作日志

## W7 闭合 + 端到端验证 + 提交整理（计划全部完成）

### 1. W7 标定：修正缺陷
- `utc_now_iso()` 日期换算错误 → 新增 `civil_from_days` + `iso8601_from_unix`（Howard Hinnant，与 `days_from_civil` 互逆）
- 3 处 `partial_cmp(..).unwrap()` 遇 NaN panic → 统一 `sorted_finite()`
- 单群体伪 ROC（`auc` 恒 None）→ 删除

### 2. 新增真实 ROC / AUC
- `ControlFamily{IidLevy, ReplayDrift, CorrelatedGaussian}` + `synthetic_control_alphas`、`build_control_families`、`control_family_summary`
- `roc_analysis(human_scores, control_scores)` / `roc_analysis_from_alphas`：阈值降序扫描、同分合并、FPR 轴梯形 AUC、Youden's J，另给 `optimal_alpha_band`
- 评分函数 `alpha_humanness_score(α) = −|α − 0.55|`
- `generate_population_report(dataset, results, control_alphas: Option<&[f64]>)`；无对照 → `roc=None`（不伪造）
- **实测离线基线**：`synth --humans 150 --control 150 --window 256` → AUC **0.9761**，判决区间 [0.0119, 1.0881]

### 3. CLI 与工具
- 新增 `gyid calibrate {synth|geolife|whitepaper}`（`trip-cli/src/calibrate_cmds.rs`）
- `tools/calibration/`：`download_geolife.sh`、`prepare_dataset.sh`、`README.md`、`.gitignore`
- `docs/CALIBRATION-W7-WHITEPAPER.md`（自动生成，含 Scope & Provenance）
- 回归基线：`trip-core/tests/calibration_golden.rs` + `tests/vectors/calibration-vectors.json`
  （重生成：`UPDATE_CALIBRATION_VECTORS=1 cargo test -p trip-core --test calibration_golden`）

### 4. 端到端浏览器验证（playwright-cli + chromium）
- 起 `trip-server`(8080) + `gyid-web`(vite 5173/5174)，注入 geolocation 走通：
  身份创建/加解锁 → GPS 采集 → GPX 导入(110 点) → 上传(116 条) → 主动验证 → PoH 签发(234B CBOR)
- **协议规则双重生效**：前端「全链自检」+ 服务端 `POST /v1/evidence` 均拒「相邻同 cell」(§4.1)；
  <64 位移样本时 Verifier 正确拒发 PoH(§7.4.1)
- 发现 UX 缺口：Collect 页允许同 cell 连续采集，违规条目会阻塞其后所有面包屑上传
- 取证 47 个文件在 `.playwright-cli/e2e-w7/`（不入库）+ `VERIFICATION.md`

### 5. 技能安装
- SkillHub + `npx skills find` 检索；安装 `data-analysis`(9814 installs) 到 `~/.codebuddy/skills/data-analysis`
- GitHub clone 方式超时失败，SkillHub 直链 zip 下载成功

### 6. 提交整理（5 个规范提交，工作区已干净）
```
12afa1b docs(w7): 标定白皮书与开发状态更新
31e9a7d chore(tools): W7 标定数据获取脚本与忽略规则
915e9fa feat(trip-cli): gyid calibrate 子命令（synth / geolife / whitepaper）
a161b70 feat(trip-core): W7 人群标定 —— 真实 ROC/AUC 对照组与白皮书生成器
61fe3ed feat(trip-server): 链上中继 GeoTITRegistry JSON-RPC + explorer 聚合修正
```
> 提交前 HEAD 是 `b80e612`（不是 `06b76e7`）；W1–W8 大部分早已提交，未提交部分只有 W7 + trip-server 中继。

### 7. 质量门禁（全绿）
- `cargo test --workspace` → **166 passed**
- `cargo clippy --workspace --all-targets -D warnings` → 0 warning
- `cargo fmt --all --check` → OK
- `contracts` 内存 EVM 测试 → 12/12
- `gyid-web` `pnpm typecheck` + `pnpm build` → 通过

### 环境备注
- 服务仍在后台运行：`trip-server`(127.0.0.1:8080)、vite(5173/5174)，日志在 `/tmp/trip-server.log`、`/tmp/vite.log`
- playwright 需 `--browser=chromium`（默认 chrome channel 在 /opt/google/chrome 不存在）
- snapshot 的 `--filename` 相对 CWD，需写 `.playwright-cli/...` 全路径
